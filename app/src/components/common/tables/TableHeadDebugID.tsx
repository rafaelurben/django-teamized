import React from 'react';

import { TableHead } from '@/shadcn/components/ui/table';

export default function TableHeadDebugID() {
    return (
        <TableHead className="debug-id tw:text-muted-foreground">ID</TableHead>
    );
}
